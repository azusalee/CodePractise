/*
 2487. 从链表中移除节点
 
 给你一个链表的头节点 head 。

 移除每个右侧有一个更大数值的节点。

 返回修改后链表的头节点 head 。
 */

struct ListNode {
    int val;
    struct ListNode *next;
}

struct ListNode* removeNodes(struct ListNode* head) {
    struct ListNode *preNode = NULL;
    
    
}
